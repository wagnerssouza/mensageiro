	(
		function () {
            
			// nao deixa criar variavel sem var
			'use strict';            
            
            // aqui setamos as variaveis que podemos usar em escopo global na nossa aplicacao
			angular.module('app').constant('url', '/');

		}
	)();